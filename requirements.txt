# Sales Bot Engine v2 — Dependencies

# Core
httpx>=0.27
pyyaml>=6.0
aiofiles>=23.0

# Telegram — choose one (userbot or bot)
telethon>=1.34       # userbot (MTProto)
# python-telegram-bot>=21.0  # bot API (legacy v1)

# VK
vk_api>=11.9

# LLM (via OpenRouter)
# No extra deps — uses httpx directly

# Optional: Пикабу scraping
beautifulsoup4>=4.12
lxml>=5.0
